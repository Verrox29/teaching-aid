import { PagePlaceholder } from '@/components/page-placeholder';

export default function NewSessionPage() {
  return (
    <PagePlaceholder
      title="Create Session"
      description="Set up a new peer-to-peer session with an admin access code."
    />
  );
}
